import { Placeholder } from "@/components/placeholder";

export default function VerificationScreen() {
  return (
    <Placeholder
      title="Verification"
      subtitle="Upload ID / license documents to get verified."
      endpoint="GET/POST /verification/documents  (⚠ verify backend)"
    />
  );
}
